{
  "name": "rspress-doc",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "rspress dev",
    "build": "rspress build",
    "preview": "rspress preview",
    "deploy": "bash ./deploy.sh"
  },
  "dependencies": {
    "rspress": "^1.0.0",
    "ts-node": "^10.9.1"
  },
  "devDependencies": {
    "@types/node": "^16"
  }
}
